# Dependencies for agentica are declared in pyproject.toml.
#
# To install the core SDK:
#     pip install -e .
#
# To install with optional extras (see pyproject.toml [project.optional-dependencies]):
#     pip install -e ".[rag]"          # RAG
#     pip install -e ".[gateway]"      # Gateway FastAPI + IM
#     pip install -e ".[cli]"          # CLI (already included in core)
#     pip install -e ".[full]"         # Everything
#
# For development:
#     pip install -e ".[dev]"
#
# This file is kept for backward compatibility only; it pins the core
# runtime deps. Prefer using pyproject.toml directly.

openai>=1.0
anthropic>=0.40
pydantic>=2.0
httpx>=0.24
Pillow>=10.0
aiofiles>=23.0
tiktoken>=0.5
pyyaml>=6.0
ruamel.yaml>=0.17
python-dotenv>=1.0
typing_extensions>=4.0
python-frontmatter>=1.0
prompt-toolkit>=3.0
rich>=13.0