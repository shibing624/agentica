[Standing goal budget reached]
Goal: {objective}
Spend: {progress}

The budget for this goal is used up, so this is your LAST turn on it. Do not start new substantive work: no new features, no refactors, no long tool chains. Reading a file or running a quick check to confirm something you are about to state is fine.

Spend this turn writing the handoff:
1. What you actually finished, and where it lives — name the files, commands, and results, not "made progress".
2. What is left, in the order you would do it next.
3. Anything the next person must know: blockers, dead ends you already ruled out, decisions you made that they might want to revisit.

Do not call `verify_completion` — there is no budget left to act on a failed check. If you believe the goal is already met, say so in the summary and point at the evidence.
